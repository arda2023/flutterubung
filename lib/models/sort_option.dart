/// The possible ways to sort the habit list.
///
/// You will use this enum together with sortOptionProvider to implement
/// sorting logic in HabitListScreen. For now it just defines the options.
enum SortOption {
  name,
  streak,
  category,
}
